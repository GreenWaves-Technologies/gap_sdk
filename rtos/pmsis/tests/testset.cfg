from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'pmsis')

    testset.add_file('api/testset.cfg')
    testset.add_file('bsp/testset.cfg')
