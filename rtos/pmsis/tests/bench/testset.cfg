from plptest import *
import os

TestConfig = c = {}

testsets = [
  'cluster/testset.cfg',
  #'periph/testset.cfg',
]

testsets += [ 'calibration/testset.cfg' ]

quick = Testset(
  name  = 'pmsis',
  files = testsets
)

c['testsets'] = [ quick ]
