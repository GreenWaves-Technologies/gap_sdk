from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'udma_fifo')

    testset.add_file('integration_tests/testset.cfg')
    testset.add_file('unit_tests/testset.cfg')
