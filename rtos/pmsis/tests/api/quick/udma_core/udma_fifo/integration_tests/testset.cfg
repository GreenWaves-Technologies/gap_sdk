from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'udma_fifo_integration_tests')

    testset.add_file('aes_both_ends/testset.cfg')
    testset.add_file('aes_both_ends_long/testset.cfg')
