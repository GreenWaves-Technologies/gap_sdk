from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'udma_core')

    testset.add_file('udma_datamove/testset.cfg')
    testset.add_file('udma_fifo/testset.cfg')
