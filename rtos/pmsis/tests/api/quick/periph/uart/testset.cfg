from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'uart')

    testset.add_file('flow_control/testset.cfg')
    testset.add_file('usart/testset.cfg')
    testset.add_file('loopback/testset.cfg')
