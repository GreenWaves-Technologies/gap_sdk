from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'unit_tests')

    testset.add_file('decompress_mode_T1/testset.cfg')
    testset.add_file('decompress_sw_fifo/testset.cfg')
