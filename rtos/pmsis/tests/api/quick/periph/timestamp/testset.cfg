from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'timestamp')
    
    testset.add_file('gpio_timestamp/testset.cfg')
    testset.add_file('tdm_timestamp/testset.cfg')
    # testset.add_file('i2c_timestamp/testset.cfg')
