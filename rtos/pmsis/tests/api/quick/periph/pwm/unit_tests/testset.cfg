from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'pwm_unit_tests')

    testset.add_file('all_pins/testset.cfg')
    testset.add_file('events/testset.cfg')
