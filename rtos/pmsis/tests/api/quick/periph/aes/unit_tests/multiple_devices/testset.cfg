from plptest import *

# Called by plptest to declare the tests
def get_tests(config):

    #
    # Test list decription
    #
    Sdk_test(config, name='aes_multiple_devices', flags='')

    #
    # Testsuites selection from tags
    #
    config.get_test('aes_multiple_devices').add_tags(['gap9_evk_testsuite'])
