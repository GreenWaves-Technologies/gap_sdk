from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'uart')

    testset.add_file('timeout_rx/testset.cfg')
    testset.add_file('timeout_tx/testset.cfg')
