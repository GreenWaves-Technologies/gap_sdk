from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'dma')

    testset.add_file('1d/testset.cfg')
    testset.add_file('2d/testset.cfg')
    testset.add_file('dmacpy/testset.cfg')
