from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'flash')

    testset.add_file('simple/testset.cfg')
    testset.add_file('with_ram/testset.cfg')
    testset.add_file('alignments/testset.cfg')
