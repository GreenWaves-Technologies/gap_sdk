from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'ram')

    testset.add_file('simple/testset.cfg')
    testset.add_file('multiple/testset.cfg')
