from plptest import *

# Called by plptest to declare the tests
def get_tests(config):
    testset = Sdk_testset(config, 'fs')

    testset.add_file('read/testset.cfg')
    testset.add_file('write/testset.cfg')
    testset.add_file('2d/testset.cfg')
