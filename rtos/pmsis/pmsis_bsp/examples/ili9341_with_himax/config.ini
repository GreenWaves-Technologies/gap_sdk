[board.devices.camera]
include = devices/camera.json
interface = cpi0
ctrl_interface = i2c1
config.image-stream=%(srcdir)s/imgTest0.pgm

[board.devices.lcd_ili9341]
include = devices/lcd_ili9341.json
interface = spim1
ctrl_interface = gpio19
cs = 0

[config]
runner.peripherals=true