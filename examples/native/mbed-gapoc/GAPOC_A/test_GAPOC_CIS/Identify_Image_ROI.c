#include "Identify_Image_ROI.h"

// Funcntion Identify_Image_ROI() processes an input image received through image_buffer
// and returns a structure containing the number of identified ROI followed by the top-left and bottom-right coordinates of each ROI.
// Max # of ROI is defined by the size of ROI_Struct_t (see MAX_NUM_ROI in Identify_Image_ROI.h)

ROI_Struct_t    Identify_Image_ROI( uint8_t* image_buffer, uint32_t Picture_Width, uint32_t Picture_Height )
{
ROI_Struct_t return_info = {0};

    // PLACEHOLDER
    // PUT IMAGE PROCESSING CODE HERE
    
//    return_info.Num_ROI = 0;
    
    return return_info;
}

