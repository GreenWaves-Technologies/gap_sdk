from plptest import *

TestConfig = c = {}

test = Test(
  name = 'test_HYPER_FLASH',
  commands = [
    Shell('all', 'make clean all run platform=rtl'),
  ],
  timeout=1000000,
)

c['tests'] = [ test ]