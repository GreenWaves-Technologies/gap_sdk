from plptest import *

TestConfig = c = {}

tests = Testset(
  name  = 'test_os',
  files = [
    'test_Delay/testset.cfg',
    'test_IRQ/testset.cfg',
    'test_EventFlags/testset.cfg',
    'test_MessageQueue/testset.cfg',
    'test_SysTick/testset.cfg',
    'test_Mutex/testset.cfg'
  ]
)

c['testsets'] = [ tests ]
