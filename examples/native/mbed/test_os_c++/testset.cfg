from plptest import *

TestConfig = c = {}

tests = Testset(
  name  = 'test_os_c++',
  files = [
    'test_EventFlags/testset.cfg',
    'test_Mail/testset.cfg',
    'test_Queue_MemoryPool/testset.cfg'
  ]
)

c['testsets'] = [ tests ]
