from plptest import *

TestConfig = c = {}

test = Test(
  name = 'task_fc_cl_stack',
  commands = [
    Shell('conf', 'make conf'),
    Shell('clean', 'make clean'),
    Shell('build', 'make all'),
    Shell('run',   'make run')
  ],
  timeout=1000000,
)
  
c['tests'] = [ test ]
