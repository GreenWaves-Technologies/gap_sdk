from plptest import *

TestConfig = c = {}

testset = Testset(
  name  = 'i2s',
  files = [
    'pdm/testset.cfg',
    'pdm_async/testset.cfg',
    'pdm_4mic/testset.cfg',
    'pcm/testset.cfg',
  ]
)

c['testsets'] = [ testset ]
