from plptest import *

TestConfig = c = {}

testset = Testset(
  name  = 'features',
  files = [
    'cluster/testset.cfg',
    'hyper_ram_delegate/testset.cfg',
    'test_malloc/testset.cfg',
    'uart_delegate/testset.cfg',
  ]
)

c['testsets'] = [ testset ]
