from plptest import *

TestConfig = c = {}

testset = Testset(
  name  = 'filesystem',
  files = [
    'filesystem/testset.cfg',
    'fs_to_l3_copy/testset.cfg',
    'littlefs/testset.cfg',
  ]
)

c['testsets'] = [ testset ]
