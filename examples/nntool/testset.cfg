from plptest import *
TestConfig = c = {}
testset = Testset(
  name  = 'nntool',
  files = [
    'mnist/testset.cfg',
    'visual_wake/testset.cfg',
  ],
  parallel=False
)
c['testsets'] = [ testset ]
