from plptest import *

TestConfig = c = {}

testset = Testset(
  name  = 'filesystem',
  files = [
    'fs_to_l3_copy/testset.cfg',
    'littlefs/testset.cfg',
    'readfs/testset.cfg'
  ]
)

c['testsets'] = [ testset ]
