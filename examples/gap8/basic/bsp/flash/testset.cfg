from plptest import *

TestConfig = c = {}

testset = Testset(
  name  = 'flash',
  files = [
    'hyper_flash/testset.cfg',
    'hyper_flash_multi_thread/testset.cfg',
  ]
)

c['testsets'] = [ testset ]
